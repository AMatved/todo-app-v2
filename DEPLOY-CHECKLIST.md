# 📋 Чек-лист для развертывания

## ✅ Перед развертыванием:

- [ ] **Сгенерировать JWT_SECRET:**
  ```bash
  node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
  ```
  Сохраните этот ключ!

- [ ] **Проверить `.gitignore`:**
  - `.env` должен быть в списке ✅ (уже есть)
  - `data/` должна быть в списке ✅ (уже есть)

- [ ] **Закоммитить код:**
  ```bash
  git add .
  git commit -m "Ready for production"
  git push
  ```

## 🚐 На хостинге (Render.com пример):

- [ ] **Создать аккаунт** на render.com через GitHub
- [ ] **Создать Web Service:**
  - Connect GitHub репозиторий
  - Runtime: Node
  - Build: `npm install`
  - Start: `node server.js`

- [ ] **Добавить Environment Variables:**
  ```
  NODE_ENV=production
  JWT_SECRET=ваш_ключ_из_шага_1
  ```

- [ ] **Дождаться деплоя** (3-5 минут)
- [ ] **Проверить:** открыть URL, проверить зеленый замок

## ⚠️ НЕ ЗАБУДЬТЕ:

- ❌ НЕ коммитить `.env` файл
- ❌ НЕ использовать дефолтный JWT_SECRET
- ❌ НЕ показывать логи с секретами
- ✅ ВСЕГДА использовать HTTPS в продакшене
- ✅ ПРОВЕРЯТЬ логи после деплоя

## 🔗 Полезные команды:

```bash
# Генерация секретного ключа
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# Проверка health endpoint
curl https://your-app.com/api/health

# Логи (Render)
# Dashboard → Your Service → Logs
```

---

## 🎯 Самый быстрый способ (3 шага):

1. **GitHub:** Залить код на github.com
2. **Render:** Создать Web Service + добавить JWT_SECRET
3. **Готово:** Приложение доступно по URL!

🎉 Поздравляем!
