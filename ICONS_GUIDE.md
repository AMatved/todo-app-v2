# Как заменить иконки на картинки

В этом приложении есть два варианта отображения иконок:
1. **Emoji** (текущий вариант, работает по умолчанию)
2. **Картинки** (изображения PNG/SVG/ICO)

## 📁 Где хранить иконки

Создайте папку `public/icons` в корне проекта и добавьте туда следующие файлы:

```
public/
└── icons/
    ├── work.png          (Работа)
    ├── study.png         (Учеба)
    ├── health.png        (Здоровье)
    ├── home.png          (Дом)
    ├── development.png   (Личное развитие)
    └── finance.png       (Финансы)
```

## 🎨 Рекомендуемый размер иконок

- **Размер:** 24x24 пикселя (можно 32x32 или 48x48)
- **Формат:** PNG (с прозрачностью) или SVG
- **Стиль:** Плоский, минималистичный
- **Цвет:** Подходит под светлую и темную темы

## 🔄 Как включить использование картинок

### Вариант 1: Заменить emoji на картинки (автоматически)

Просто добавьте файлы иконок в папку `public/icons` и обновите код в `client.js`:

**Найдите строку ~865 в файле `client.js`:**

```javascript
// Для поддержки картинок: добавьте URL чтобы использовать картинки вместо emoji
const categoryIconImages = {
  work: '/icons/work.png',
  study: '/icons/study.png',
  health: '/icons/health.png',
  home: '/icons/home.png',
  development: '/icons/development.png',
  finance: '/icons/finance.png'
};
```

**Замените на:**

```javascript
// Использовать картинки вместо emoji
categorySelectorIcon.innerHTML = `<img src="${categoryIconImages[category]}" alt="${category}">`;
```

Вместо:
```javascript
categorySelectorIcon.textContent = categoryIcons[category];
```

### Вариант 2: Использовать background-image (CSS)

Добавьте в `styles.css`:

```css
.category-icon[data-category="work"],
.category-selector-icon[data-category="work"] {
  background: url('/icons/work.png') center/contain no-repeat;
}

.category-icon[data-category="study"],
.category-selector-icon[data-category="study"] {
  background: url('/icons/study.png') center/contain no-repeat;
}

/* и так далее для всех категорий */
```

## 🎯 Где найти бесплатные иконки

### Рекомендуемые ресурсы:

1. **Flaticon** - https://www.flaticon.com/
   - Огромная коллекция бесплатных иконок
   - Форматы: PNG, SVG, ICO
   - Нужно указать автора

2. **Icons8** - https://icons8.com/
   - Качественные иконки
   - Бесплатно для личного использования
   - Можно изменить цвет

3. **IconFinder** - https://www.iconfinder.com/
   - Множество стилей
   - Фильтр по цене (бесплатные)

4. **Font Awesome** - https://fontawesome.com/
   - SVG иконки
   - Можно скачать отдельные файлы

5. **Heroicons** - https://heroicons.com/
   - Бесплатные SVG иконки
   - От Twitter
   - Минималистичный стиль

### Иконки для категорий (примеры):

**Работа (Work):**
- 💼 Портфель
- 🏢 Офисное здание
- 👔 Галстук
- 💻 Компьютер

**Учеба (Study):**
- 📚 Книги
- 🎓 Шапка выпускника
- ✏️ Карандаш
- 📖 Открытая книга

**Здоровье (Health):**
- 💪 Динамо
- ❤️ Сердце
- 🏥 Больница
- 🥗 Здоровая еда

**Дом (Home):**
- 🏠 Дом
- 🛋️ Диван
- 🔑 Ключ
- 🏡 Домик

**Личное развитие (Development):**
- 🚀 Ракета
- 📈 График роста
- 💡 Лампочка
- 🎯 Цель

**Финансы (Finance):**
- 💰 Деньги
- 💳 Карточка
- 📊 График
- 💎 Кристалл

## 🚀 Быстрый старт

### Шаг 1: Скачайте иконки

1. Перейдите на https://www.flaticon.com/
2. Скачайте 6 иконок (PNG, 48px, для каждой категории)
3. Переименуйте файлы:
   - `work.png`
   - `study.png`
   - `health.png`
   - `home.png`
   - `development.png`
   - `finance.png`

### Шаг 2: Добавьте в проект

1. Создайте папку `public/icons` если её нет
2. Переместите файлы иконок в эту папку

### Шаг 3: Обновите код

В файле `client.js` (~строка 894), замените:

```javascript
categorySelectorIcon.textContent = categoryIcons[category];
```

На:

```javascript
// Использовать картинки вместо emoji
categorySelectorIcon.innerHTML = `<img src="${categoryIconImages[category]}" alt="${category}">`;
```

И в файле `client.js` (~строка 1003), замените:

```javascript
iconSpan.textContent = categoryIcons[taskData.category];
```

На:

```javascript
iconSpan.innerHTML = `<img src="${categoryIconImages[taskData.category]}" alt="${taskData.category}">`;
```

### Шаг 4: Перезапустите приложение

```bash
npm start
```

Готово! 🎉 Теперь ваши иконки - это картинки вместо emoji.

## 📝 Примечание

Если вы не добавите картинки в папку `public/icons`, приложение продолжит работать с emoji иконками без ошибок.

## 🎨 Адаптация под тему

Чтобы иконки выглядели хорошо в светлой и темной теме, используйте:

- **PNG с прозрачностью**
- **Нейтральные цвета** (серый, черный)
- **SVG** (можно менять цвет через CSS)

Или создайте два набора иконок:
- `work-light.png` - для светлой темы
- `work-dark.png` - для темной темы

И переключайте их через JavaScript в зависимости от темы.
