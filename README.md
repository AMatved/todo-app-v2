# 🔐 Secure Todo List Application

Приложение для управления задачами с безопасной аутентификацией пользователей, хэшированием паролей и поддержкой HTTPS.

## 🛡️ Безопасность

### Реализованные меры защиты:

1. **Хэширование паролей с bcrypt**
   - Используется bcrypt с cost factor = 12 (2^12 = 4096 итераций)
   - Соль генерируется автоматически и включается в хэш
   - Timing-attack безопасное сравнение паролей

2. **JWT токены для сессий**
   - Токены хранятся в httpOnly cookies (защита от XSS)
   - SameSite=strict защита от CSRF
   - Срок действия: 7 дней

3. **HTTPS шифрование**
   - Самоподписанный SSL сертификат
   - Шифрование всего трафика между клиентом и сервером
   - HTTP сервер также доступен для разработки

4. **Rate Limiting**
   - Защита от bruteforce атак на endpoints аутентификации
   - Максимум 5 попыток за 15 минут для /api/auth/*
   - Общий лимит: 100 запросов за 15 минут

5. **SQL Injection защита**
   - Параметризованные запросы в SQLite
   - Input validation на сервере

6. **XSS защита**
   - HTTP заголовки безопасности через Helmet
   - Content Security Policy
   - Экранирование HTML на клиенте

7. **CORS конфигурация**
   - Запросы принимаются только с разрешенных доменов

## 📋 Требования

- Node.js (v14 или выше)
- npm (v6 или выше)

## 🚀 Установка и запуск

### Шаг 1: Установка зависимостей

```bash
npm install
```

При установке автоматически сгенерируется SSL сертификат.

### Шаг 2: Конфигурация (опционально)

Файл `.env` уже создан с настройками по умолчанию. Вы можете изменить:

```env
PORT=3000                    # HTTP порт
HTTPS_PORT=3443              # HTTPS порт
JWT_SECRET=ваш-секретный-ключ  # ОБЯЗАТЕЛЬНО изменить в продакшене!
JWT_EXPIRES_IN=7d            # Срок действия токена
```

**⚠️ ВАЖНО:** Для продакшена сгенерируйте новый JWT_SECRET:

```bash
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

### Шаг 3: Запуск сервера

**Режим разработки (с автоматическим перезапуском):**
```bash
npm run dev
```

**Обычный запуск:**
```bash
npm start
```

Сервер будет доступен по двум адресам:
- **HTTP:** http://localhost:3000
- **HTTPS:** https://localhost:3443

### Шаг 4: Использование приложения

1. Откройте http://localhost:3000 в браузере
2. Зарегистрируйте новый аккаунт или войдите как гость
3. Создавайте и управляйте задачами

## 📁 Структура проекта

```
todo-list-project/
├── server.js              # Главный файл сервера
├── database.js            # Модуль работы с SQLite
├── package.json           # Зависимости и скрипты
├── .env                   # Конфигурация (создается автоматически)
├── .gitignore            # Игнорируемые файлы
│
├── middleware/
│   └── auth.js           # JWT аутентификация
│
├── routes/
│   ├── auth.js           # API аутентификации
│   └── tasks.js          # API управления задачами
│
├── scripts/
│   └── generate-cert.js  # Генерация SSL сертификата
│
├── certs/                # Папка для SSL сертификатов
│   ├── key.pem          # Приватный ключ
│   └── cert.pem         # Сертификат
│
├── data/                 # Папка для базы данных
│   └── todo-app.db      # SQLite база данных
│
├── public/              # Статические файлы (символическая ссылка на корень)
│   ├── index.html      # Главная страница
│   ├── styles.css      # Стили
│   └── client.js       # Клиентский JavaScript
│
├── index.html          # Главная страница
├── styles.css          # Стили
├── client.js           # Новый клиент (работа с API)
└── script.js           # Старый клиент (localStorage)
```

## 🔌 API Документация

### Аутентификация

#### POST /api/auth/register
Регистрация нового пользователя.

**Request:**
```json
{
  "username": "user123",
  "password": "securepass"
}
```

**Response (201):**
```json
{
  "message": "User registered successfully",
  "user": {
    "id": 1,
    "username": "user123"
  },
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

#### POST /api/auth/login
Вход пользователя.

**Request:**
```json
{
  "username": "user123",
  "password": "securepass"
}
```

**Response (200):**
```json
{
  "message": "Login successful",
  "user": {
    "id": 1,
    "username": "user123"
  },
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

#### POST /api/auth/logout
Выход пользователя (удаляет cookie).

**Response (200):**
```json
{
  "message": "Logged out successfully"
}
```

#### GET /api/auth/me
Получить информацию о текущем пользователе.
Требует аутентификации.

**Response (200):**
```json
{
  "user": {
    "id": 1,
    "username": "user123",
    "created_at": "2025-01-04 12:00:00",
    "last_login": "2025-01-04 14:30:00"
  }
}
```

### Задачи

Все endpoints требуют аутентификацию (Bearer token или cookie).

#### GET /api/tasks
Получить все задачи текущего пользователя.

**Response (200):**
```json
{
  "tasks": [
    {
      "id": 1,
      "text": "Buy groceries",
      "completed": false,
      "created_at": "2025-01-04 10:00:00",
      "updated_at": "2025-01-04 10:00:00"
    }
  ]
}
```

#### POST /api/tasks
Создать новую задачу.

**Request:**
```json
{
  "text": "New task"
}
```

**Response (201):**
```json
{
  "message": "Task created successfully",
  "task": {
    "id": 2,
    "text": "New task",
    "completed": false,
    "created_at": "2025-01-04 11:00:00",
    "updated_at": "2025-01-04 11:00:00"
  }
}
```

#### PUT /api/tasks/:id
Обновить задачу.

**Request:**
```json
{
  "text": "Updated task",
  "completed": true
}
```

**Response (200):**
```json
{
  "message": "Task updated successfully"
}
```

#### DELETE /api/tasks/:id
Удалить задачу.

**Response (200):**
```json
{
  "message": "Task deleted successfully"
}
```

## 🔧 Пересоздание SSL сертификата

Если нужно пересоздать сертификат:

```bash
npm run generate-cert
```

## ⚠️ Предупреждение браузера о сертификате

При использовании HTTPS (https://localhost:3443) браузер покажет предупреждение о небезопасном соединении.

**Это нормально!** Самоподписанный сертификат не доверяется браузерами автоматически.

**Как обойти:**
1. Chrome: Нажмите "Advanced" → "Proceed to localhost (unsafe)"
2. Firefox: Нажмите "Advanced" → "Accept the Risk and Continue"
3. В продакшене используйте сертификат от Let's Encrypt (бесплатно)

## 🚀 Продакшен развертывание

Для продакшена необходимо:

1. **Изменить JWT_SECRET** на случайный 256-битный ключ
2. **Получить настоящий SSL сертификат** (Let's Encrypt)
3. **Установить NODE_ENV=production**
4. **Использовать PM2** для управления процессами:
   ```bash
   npm install -g pm2
   pm2 start server.js --name todo-app
   pm2 startup
   pm2 save
   ```
5. **Настроить обратный прокси** (Nginx/Apache)
6. **Открыть порты** в файрволе

## 🐛 Решение проблем

### Ошибка: "Cannot find module 'sqlite3'"
```bash
npm rebuild sqlite3
```

### Ошибка: "SSL certificates not found"
```bash
npm run generate-cert
```

### Порт уже занят
Измените PORT в `.env` файле.

### База данных не создается
Проверьте права на запись в папку `data/`.

## 📝 Лицензия

ISC

## 👨‍💻 Автор

Создано с использованием Node.js, Express, SQLite, bcrypt, JWT.

---

**Примечание:** Это учебный проект. Для продакшена требуются дополнительные меры безопасности, включая регулярные обновления зависимостей, аудит безопасности и настройку WAF.
