# 🚀 Быстрый старт

## Запуск приложения

```bash
npm start
```

## Доступ к приложению

После запуска сервер будет доступен по двум адресам:

### HTTP (для разработки)
http://localhost:3000

### HTTPS (безопасное соединение)
https://localhost:3443

⚠️ **При открытии HTTPS браузер покажет предупреждение** - это нормально для самоподписанного сертификата.

### Как обойти предупреждение браузера:

**Chrome/Edge:**
1. Нажмите "Advanced" (Дополнительно)
2. Нажмите "Proceed to localhost (unsafe)" (Перейти на localhost (небезопасно))

**Firefox:**
1. Нажмите "Advanced" (Дополнительно)
2. Нажмите "Accept the Risk and Continue" (Принять риск и продолжить)

## Тестирование

1. Откройте приложение
2. Создайте аккаунт (минимум 6 символов для пароля)
3. Войдите в систему
4. Создайте несколько задач
5. Проверьте сохранение после перезагрузки страницы

## Особенности

✅ **Безопасное хранение паролей** - bcrypt с 4096 итерациями
✅ **JWT токены** - защищенные сессии
✅ **HTTPS** - шифрование всего трафика
✅ **Rate limiting** - защита от перебора паролей
✅ **SQL injection защита** - параметризованные запросы
✅ **XSS защита** - экранирование и CSP

## Остановка сервера

Нажмите `Ctrl + C` в терминале

## Режим разработки (с автоперезагрузкой)

```bash
npm run dev
```

Сервер будет автоматически перезапускаться при изменении файлов.

## Что изменилось?

### Старая версия (script.js):
- Данные хранились в localStorage браузера
- Пароли сохранялись в открытом виде
- Без шифрования
- Уязвимости безопасности

### Новая версия (client.js + server.js):
- Данные хранятся в SQLite базе данных на сервере
- Пароли хэшируются с bcrypt (необратимо)
- HTTPS шифрование
- JWT токены для сессий
- Rate limiting и другие меры защиты

## API endpoints

```
POST /api/auth/register - Регистрация
POST /api/auth/login    - Вход
POST /api/auth/logout   - Выход
GET  /api/auth/me       - Текущий пользователь

GET    /api/tasks       - Получить задачи
POST   /api/tasks       - Создать задачу
PUT    /api/tasks/:id   - Обновить задачу
DELETE /api/tasks/:id   - Удалить задачу
```

## Полная документация

См. [README.md](README.md) для подробной информации.
